[package]
name = "gamepad-proxy"
version = "0.1.0"
edition = "2021"

[dependencies]
evdev = "0.13"
clap = { version = "4.4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"
tempfile = "3"

